# M1.10 — Display Settings and Timed Keep/Revert

September 6, 2026. Complete source increment based on actual M1.9; no native build or executable.

- Added UI-owned, explicitly enabled Windows display adapter, reported resolution/window-mode catalogue and separate native Display/ConfirmDisplay panels.
- Added real-time 15-second trial, actual pretrial rollback snapshot, post-match presentation gate, later-frame observed restoration and five-second restoration-failure lock.
- Added session-only Keep and explicit standard engine settings save request, without campaign/player-preference migration or automatic trial writes.
- Added four unrun native tests, a standalone display suite, 38 unrun manual scenarios, wiring and whole-project remaining-work review.
- Preserved prior gameplay systems and data; archived original M1.9 evidence rather than attributing it to new native execution.

---

# M1.9 — Ambience and transcript playback source (2026-09-06)

- Add one optional native controller playback owner for a real looping ambience bed and finite radio recording.
- Integrate with existing top-panel presentation, session epochs, pause/return and immediate cancellation hooks.
- Submit radio Play once per displayed ticket; never auto-retry or drive mission completion from audio.
- Use captured dedicated sound classes with unit source gain; do not multiply Master twice or change options format.
- Stop owned voices before removing volume routing; make release notification reentry safe.
- Add shared rules, 111 standalone assertions, four unrun native tests, 34 unrun integration cases and wiring/source documentation.
- Keep all campaign, inventory, mission, preference codec, recovery and movement rules unchanged.
- Source verification is separate from unrun Unreal/audio/vendor/Windows gates. No audio assets or executable supplied.

---

# M1.8 — Routed Audio Options / 2026-09-06

- Added four 0–100% volume fields and optional local-controller native SoundMix owner using actual dedicated sound classes.
- Added captured-device, one-push/one-pop lifecycle, changed-only gain submissions, disabled-unbound controls and audio diagnostics.
- Preserved transcript/mission independence, existing input ownership and all campaign/inventory/receipt source.
- Read preference schemas 1/2/3, write schema 3 explicitly; froze an actual original M1.7 encoder fixture and added upgrade/failure checks.
- Added two standalone suites and four unrun Unreal tests; current test inventory is 16 standalone / 30 native.
- Updated current codec size/version and ten-row cycling expectations in retained suites; prior feature rules remain tested.
- Included full wiring/specification/acceptance/evidence. No engine build, audio assets, rendered capture or playable package is claimed.

---

# M1.7 — Walk/sprint and preference upgrade

- Added optional fixed-character speed owner with 330/520 cm/s defaults, actual aggregate Sprint samples, Hold/Toggle and deliberate post-transition rearming.
- Added two-frame freshness, duplicate-sample protection and guarded teardown/foreign-speed detection without another movement framework or input context.
- Added the sixth native options field; existing draft, session-only and verified-save controls remain.
- Added strict schema-1 read compatibility and explicit schema-2 preference upgrades; no automatic rewrite or campaign schema change.
- Generated a frozen fixture with the actual supplied M1.6 encoder; added two standalone suites and four unrun Unreal tests.
- Retained all campaign/vendor/recovery contracts, story and IDs. M1 remains unpassed; no coast or Windows executable included.

---

# M1.6 — Player options and readability

- Added native draft/apply/save options for text size, mouse/stick look, invert-Y and FOV.
- Added optional fixed-character camera/look consumer with explicit host-input ownership; no new mapping context or vendor symbols.
- Added independent bounded local preference codec, alternating read-back-verified IO, read-change protection and session-only fallback.
- Moved complete native modal contents into a scrollable layout; added scaled fonts, focused-command scrolling and body-visibility presentation checks.
- Retained campaign/AGIS/Hyper/recovery ownership, world IDs, story and existing acceptance gates.
- Added three standalone suites and three unrun Unreal tests; archived prior M1.5 delivery evidence.
- Unreal/vendor/controller/Windows validation remains unrun; M1 not passed; no M2 coast included.

---

# M1.5 — Safe return and dry checkpoints

- Added native character recovery, authored safety volumes, checkpoint dwell, bounded fallback placement and stable-ground verification.
- Added exclusive coordinator recovery scope; retained pending autosaves and coherent existing snapshot/ledger ownership.
- Added a temporary gameplay input-stack blocker, camera fade ownership and native UI notices.
- Integrated optional recovery with existing bootstrap; added opt-in safety layout to the non-overwriting room generator.
- Tightened checkpoint mutation lifecycle and actual-transform validation after return/load teleport fitting.
- Added shared helper tests, recipe tests and four unrun Unreal automation tests.
- Preserved prior reports/logs under archive paths. Unreal/vendor/Windows acceptance remains unrun.

---

# M1.4 — 0.1.4-m1-interaction-source

- Added original controller-owned interaction relay with mutually exclusive vendor-state and optional native E/face-left input routes.
- Added read-only Hyper prompt projection, current-target leases and permission/session invalidation.
- Fixed same-frame and action-notice reentrant dispatch across world interaction, transfer and transcript acknowledgement.
- Tightened direct world-interaction checks for paused/recovery states and actual possessed local player.
- Integrated an optional relay into the checked bootstrap; legacy M1.3 hosts without a relay remain explicit legacy paths.
- Added one standalone interaction-rule suite, four unrun Unreal automation tests and a separate local acceptance ledger.
- Retained AGIS fail-closed defaults, campaign/save schemas, world/item IDs, first-signal story and all earlier tests.
- Unreal, AGIS/Hyper runtime wiring, rendered prompts, Windows build/package and gameplay acceptance remain not run.

# M1.3 — Checked startup source

- Added explicit controller-owned startup with read-only preflight, repeat/reentry guards and terminal partial-binding failures.
- Added exact M1 authoring and real provisional-provider read diagnostics; retained the unconfigured default adapter.
- Shared the existing dry-placement check between startup and restore, with finite capsule guards.
- Added nonmutating loaded-editor-world audit and exclusive-new-file manifest-only reports.
- Tightened partial menu-input installation cleanup.
- Added 287 standalone assertions, 12 Python report cases, four unexecuted Unreal tests and 26 local acceptance cases.
- Preserved M1.2/M1 source, contracts and historical evidence. No campaign schema/ID migration, vendor assets, compiled Unreal game or coastline map.

---

# Source changelog

## 0.1.2-m1-ui-source

Added native C++ UMG session, pause, journal, radio transcript, inventory/storage list, inspection, confirmation and fatal-recovery screens; a controller-owned UI stack; three runtime menu actions; save-set discovery; live AGIS view validation; explicit transfer-retry handling; a read-only local preflight tool; new shared-rule and Python tests; and local integration/acceptance records.

Extended `UCoastalInventoryAdapter` with fail-closed `ReadContainerView`. Added read-only coordinator status/save-set accessors and a native bridge storage-context check. Bridge configuration is now one-time and requires a configured coordinator. Existing save schemas, envelope protocol, mission rules, items and world IDs are unchanged.

The package does not implement the purchased AGIS/Hyper APIs or supply a host project. No Unreal compilation, widget rendering, actual controller session, Windows build or gameplay certification is claimed.

## 0.1.1-m1-source

Retained previous mission, coherent campaign persistence, guarded interactions, world proxies, editor map recipe, source tests and original integration guidance. Earlier reports are archived explicitly as earlier evidence.
