# Coastal Exploration — What Remains After M1.10

> Current M2 implementation is complete with a built Windows archive. See [M2 implementation](M2_IMPLEMENTATION.md) and [scope audit](M2_SCOPE_AUDIT.md). The user deferred gameplay testing; M2's gameplay gate remains unrun. Remaining development proceeds to M3 presentation/input/stability and M4 tested delivery.

> Historical source-package assessment retained below. For current native progress, use [M1 native status](M1_NATIVE_INTEGRATION_STATUS.md) and [closeout plan](M1_NATIVE_INTEGRATION_PLAN.md): the compiled real AGIS/Hyper packaged loop passes, Nordic Hut is downloaded, and M1 implementation is closed with user-directed deferral of cabin compatibility and practical device tests. The older statements below that native runs were unperformed describe the imported delivery, not the current workspace.

**Review date:** September 6, 2026, America/New_York  
**Basis:** Supplied foundation and M1.9 package; current source increment M1.10.  
**Product:** Third-person, single-player coastal exploration; Windows first; opening chapter First Signal.

## Current position

There is a substantial original source foundation, not yet a demonstrated playable game. The latest supplied report explicitly left the actual Unreal build, vendor integration, native rendering/input/audio, real platform IO and launched Windows campaign loop unrun. This delivery adds another bounded presentation feature and reruns source checks; it does not change that native acceptance status.

Implemented source includes the First Signal mission and journal/transcript logic, campaign coordination and resilient save envelopes, inventory adapter contracts, persistent development objects, native session/pause/inventory/storage/journal screens, startup checks, interaction routing, safe return/checkpoints, camera/look/text settings, Hold/Toggle sprint, local player-preference upgrades, routed volume controls and optional ambience/radio playback. M1.10 adds native display drafts and timed Keep/Revert. These claims describe source, not tested real AGIS/Hyper gameplay or final art.

No honest overall-completion percentage follows from assertion counts. The largest remaining uncertainty is the real engine/vendor integration, not whether another standalone helper test passes.

## M1 — Finish the working test room

| Required work | Concrete completion evidence |
|---|---|
| Select actual compatible Unreal/toolchain/vendor versions | Real installed version record and successful compatibility test of the chosen packages. A current documentation version is not a project pin. |
| Build native project/plugin | Actual UHT/UBT and Windows compiler output, resolving any reflected/API defects that source-only checks cannot find. |
| Implement and bind AGIS adapter | Actual pickup, export/restore, inventory/storage transfer, atomic repair consumption, rollback and item conservation. No mock provider replacing the selected inventory system. |
| Bind Hyper and actual input | Real selected target/prompt route, held/released state, reach/occlusion, fresh input after menus/load/recovery, keyboard/mouse and controller. |
| Build the systems room in-engine | Run and inspect the supplied editor recipe, configure the actual pawn/controller/director/bootstrap, validate floor, collision, checkpoints and hazards. No `.umap` has been generated in this delivery. |
| Validate native presentation consumers | Real look/FOV, sprint, scrolling/text sizes, audio routing/playback and display trial/rollback. Optional opt-in flags alone do not prove wiring. |
| Validate real save IO and failure behavior | Disposable-profile tests for campaign and preference slots, corruption/future versions, refused/ambiguous writes, relaunch and old-save compatibility; engine display persistence separately. |
| Complete launched Windows loop | New → collect both parts → storage round-trip → repair radio → explicit transcript acknowledgement → safe return → verified save → quit/relaunch → Continue with coherent world and inventory. |

All 38 supplied native automation tests and the actual-host ledgers remain unrun here. Helper-style engine tests alone would still not establish the whole gameplay gate. Retain exact logs, screenshots, controller observations and real file/asset/version provenance.

**This is the next required production step.** Additional M3 source features do not replace it or permit declaring M1 passed.

## M2 — Assemble the coastal opening

After the real M1 gate, build the compact handcrafted route in the foundation: **Cabin Cove → Shoreline Trail → Old Dock**, with **Rail Overlook optional**. Place the cabin return point, storage, journal/radio, battery/fuse, notes/discoveries and route guidance in that actual world. Preserve the selected grounded/light-mystery tone and existing First Signal story.

Import and integrate owned environment assets, choose the real playable character/animation setup, replace DEV PROXY presentation, finish collision and camera clearance, and make required objects and paths readable. Check traversal pacing, return paths, hidden collision traps and safe boundaries. Avoid an empty oversized coast or unrelated combat/survival systems.

The package contains source and editor recipes, not purchased content, a finished coast, final character art or an offshore continuation. Future assets/leads must not silently expand the initial playable scope.

## M3 — Finish presentation, input and stability

M3 is now in progress. See [M3 implementation](M3_IMPLEMENTATION.md) for the current native UI increment and remaining work. The table below describes the historical source-delivery gaps; current M1/M2 integration and audio evidence supersede claims that no host or sound files exist locally.

| Area | What remains after this source update |
|---|---|
| Display/graphics | M1.10's resolution/window-mode source now exists. Native switching, timeout/rollback, controller/readability, DPI/monitor and engine-file fault tests remain. Quality presets, VSync/frame caps, render scale/dynamic resolution, HDR and monitor-selection controls are not supplied by this increment. |
| Input/accessibility | Actual controller-only end-to-end use, final prompts/glyphs and intended remapping, consistent native/vendor text, comfortable camera collision/interiors, sensitivity/inversion/sprint behavior, readable full transcripts and accessibility review. Existing native text scaling is not a complete accessibility certification. |
| Audio | Assign/approve real owned ambience/radio sources and effects, implement/integrate footsteps with actual locomotion/surfaces, refine environmental transitions and balance, verify mute/paused behavior and voice lifetimes. No sound files are included. |
| Art and feedback | Final character/environment integration, item icons, coherent lighting/materials and clear interaction/objective feedback, replacing development presentation where required. |
| Performance and reliability | Measure real frame times, memory and native IO hitches on the intended hardware; profile loaded assets, collision/lighting/audio and packaged builds; fix leaks, repeated-load failures, camera/input/UI regressions and launch/save issues. Foundation performance numbers remain targets, not measurements. |

Some of these features were deliberately brought forward into M1 source increments. That does not mean every M3 requirement was already implemented or tested. The display update does not claim an entire graphics menu or a production-ready video-recovery system.

## M4 — Deliver and validate the Windows build

Produce the actual game executable/package, test clean launch on the target setup outside the editor, and run the complete first-chapter acceptance/failure loop. Deliver a controls/install guide, actual dependency/version record, asset provenance, known-issues list and backed-up save/settings recovery instructions alongside genuine test evidence. No playable executable is included here.

## Scope held deliberately outside the first build

The foundation excludes co-op/network accounts, combat/enemies, hunger/thirst, crafting trees/construction, swimming/boats, dynamic tides and an economy. They are not missing pieces that must be added to finish this opening. More purchased assets do not automatically change these decisions.

## Reading map

The original [foundation](COASTAL_EXPLORATION_FOUNDATION.md) is the milestone authority. The [historical M1.9 validation](archive/M1_9_VALIDATION_REPORT.md) records the last supplied state. The current [M1.10 implementation](M1_10_IMPLEMENTATION.md), [wiring](M1_10_DISPLAY_WIRING.md) and [validation](../VALIDATION_REPORT.md) separate this increment's source work from the still-unrun native gate.
