# M1 native integration plan

Completed evidence includes baseline verification/import; native UE 5.7.4 builds; systems room; actual AGIS and Hyper inspection and wrappers; one bootstrap and native interaction input; 41 native tests; packaged automated New and fresh-process Continue; partial and full-container saves; selected failure cases; display persistence; preference persistence; measured audio gains and live Master mute/restoration.

M1 implementation closeout is complete under the user's September 7 direction to download the cabin and defer testing until the game is more fully built. The existing flagged repair-staging native test passed; no gameplay fix or rebuild was needed. Disposition:

1. Nordic Hut acquisition is complete through Epic Games Launcher into external `LocalVendor/NordicHut`, with a UE 5.7 staging descriptor. Cabin runtime compatibility testing is deferred by user; installation success does not prove compatibility.
2. The practical keyboard/mouse and physical-controller New-to-Continue route is deferred by user. Existing injected input evidence remains valid but does not prove physical operation.
3. Build instructions and reconciliation are in the workspace `M1_HANDOFF.md`. Retain deferred checks for later validation, without reopening this implementation closeout or adding unrelated tests. M2 scene assembly remains separate work.

No publication is authorized. Paid content and all generated host/package/save/evidence files remain outside the public source repository.
